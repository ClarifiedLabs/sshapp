# SSH App

SSH App is a SwiftUI SSH client for iPhone and iPad. It uses libghostty for
terminal rendering, libssh2 for SSH transport, and supports tmux control mode
(`tmux -CC`).

## Development

- Setup: `make setup`
- Build: `make build`
- Test: `make test`
- Release: see `docs/RELEASES.md`

## License

MIT
